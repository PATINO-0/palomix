name: palomix
description: "Palomix - App de películas con chat y favoritos"
publish_to: "none"
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # Supabase
  supabase_flutter: ^2.5.0

  # HTTP for TMDb and Groq
  http: ^1.2.0

  # Env vars
  flutter_dotenv: ^5.1.0

  equatable: ^2.0.5

  # Animations & UI helpers (opcionales, todas libres)
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/groq/prompt_movie_summary.json
    - assets/config/tmdb.json
    - .env
